'use client'

/**
 * Stub — memory operations now live in lib/memory/{store,consolidate,context}.
 * Kept for backwards-compat with old imports during the transition.
 */
export function useMemory() {
  return {}
}
